import { SeparateLettersPipe } from './separate-letters-pipe';

describe('SeparateLettersPipe', () => {
  it('create an instance', () => {
    const pipe = new SeparateLettersPipe();
    expect(pipe).toBeTruthy();
  });
});
